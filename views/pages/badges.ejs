<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CleanTech - Recyclage Intelligent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/m.css" />
</head>
<style>
:root {
--primary: #10b981;--primary-dark: #059669;
--primary-light: #d1fae5;
--secondary: #3b82f6;
--secondary-dark: #2563eb;
--accent: #f59e0b;
--dark: #1f2937;
--light: #f9fafb;
}
.dashboard-card {
background: white;
border-radius: 15px;
box-shadow: 0 4px 20px rgba(0,0,0,0.08);
transition: all 0.3s ease;
border: none;
overflow: hidden;
}
.dashboard-card:hover {
transform: translateY(-5px);
box-shadow: 0 8px 30px rgba(0,0,0,0.12);
}
.stat-card {
background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
color: white;
border-radius: 15px;
padding: 1.5rem;
}
.nav-pills-custom .nav-link {
border-radius: 10px;
margin: 0.2rem 0;
transition: all 0.3s ease;
}
.nav-pills-custom .nav-link.active {
background: var(--primary);
color: white;
}
</style>
</head>
<body>

<%- include('../partials/navbar', { user}) %>
<br>
  <main style="padding-top: 80px;">
<div class="container py-5">
<div class="row">
<div class="col-12">
<div class="d-flex justify-content-between align-items-c
enter mb-5">
<div>
<h1 class="h2 fw-bold text-dark mb-2">Badges Disponibles</h1>
<p class="text-muted">Débloquez des badges en accomplissant des défis écologiques</p>
</div>
<div class="bg-primary text-white rounded-pill px-3 py-1">
<small><i class="fas fa-medal me-1"></i>Badges</small>
</div>
</div>
</div>
</div>
<div class="row">
<% if (badges.length === 0) { %>
<div class="col-12">
<div class="dashboard-card p-5 text-center">
<i class="fas fa-medal fa-4x text-muted mb-3"></i>
<h5 class="text-muted">Aucun badge disponible</h5>
<p class="text-muted">Les badges seront bientôt disponibles !</p>
</div>
</div>
<% } else { %>
<% badges.forEach(b => { %>
<div class="col-lg-4 col-md-6 mb-4">
<div class="dashboard-card p-4 h-100 text-center"><div class="badge-icon mx-auto mb-3">
<div class="bg-<%= b.tier === 1 ? 'success' : b.tier === 2 ? 'warning' : 'danger' %> rounded-circle d-flex align-items-center justify-content-center mx-auto" style="width: 80px; height: 80px;">
<i class="fas fa-<%= b.type === 'parrainage' ? 'users' : b.type === 'impact' ? 'leaf' : b.type === 'defi' ? 'trophy' : 'star' %> fa-2x text-white"></i>
</div>
</div>
<h5 class="fw-bold mb-2"><%= b.name %></h5>
<p class="text-muted mb-3"><%= b.description %></p>
<div class="d-flex justify-content-center gap-2 mb-3">
<span class="badge bg-<%= b.tier === 1 ? 'success' : b.tier === 2 ? 'warning' : 'danger' %>">
Niveau <%= b.tier %>
</span>
<span class="badge bg-primary">
<%= b.requirement_points %> points
</span>
</div>
<div class="progress mb-3" style="height: 8px;">
<div class="progress-bar bg-<%= b.tier === 1 ? 'success' : b.tier === 2 ? 'warning' : 'danger' %>" style="width: <%= Math.random() * 100 %>%">
</div>
</div>
<small class="text-muted">
<%= Math.floor(Math.random() * 100) %>% des utilisateurs ont ce badge</small></div>
</div>
<% }) %>
<% } %>
</div>
</div>
</main>
  <%- include('../partials/footer') %>

