<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CleanTech - Recyclage Intelligent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/m.css" />
</head>
<style>
:root {
--primary: #10b981;--primary-dark: #059669;
--primary-light: #d1fae5;
--secondary: #3b82f6;
--secondary-dark: #2563eb;
--accent: #f59e0b;
--dark: #1f2937;
--light: #f9fafb;
}
.dashboard-card {
background: white;
border-radius: 15px;
box-shadow: 0 4px 20px rgba(0,0,0,0.08);
transition: all 0.3s ease;
border: none;
overflow: hidden;
}
.dashboard-card:hover {
transform: translateY(-5px);
box-shadow: 0 8px 30px rgba(0,0,0,0.12);
}
.stat-card {
background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
color: white;
border-radius: 15px;
padding: 1.5rem;
}
.nav-pills-custom .nav-link {
border-radius: 10px;
margin: 0.2rem 0;
transition: all 0.3s ease;
}
.nav-pills-custom .nav-link.active {
background: var(--primary);
color: white;
}
</style>
</head>
<body>

<%- include('../partials/navbar', { user}) %>
  <br><br>
  <main style="padding-top: 80px;">
<div class="container py-5">
<div class="row">
<div class="col-12">
<div class="d-flex justify-content-between align-items-center mb-5">
<div>
<h1 class="h2 fw-bold text-dark mb-2">Mes Dépôts</h1>
<p class="text-muted">Historique de tous vos dépôts de déchets</p>
</div>
<a href="/deposit-points" class="btn btn-primary">
<i class="fas fa-map-marker-alt me-2"></i>Trouver un point de collecte
</a>
</div>
</div>
</div><div class="row">
<div class="col-12">
<div class="dashboard-card p-4">
<% if (deposits.length === 0) { %>
<div class="text-center py-5">
<i class="fas fa-recycle fa-4x text-muted mb-3"></i>
<h5 class="text-muted">Aucun dépôt pour le moment
</h5>
<p class="text-muted mb-4">Commencez à recycler etgagnez vos premiers points !</p>
<a href="/deposit-points" class="btn btn-primary">
<i class="fas fa-map-marker-alt me-2"></i>Trouver un point de collecte
</a>
</div>
<% } else { %>
<div class="table-responsive">
<table class="table table-hover">
<thead class="table-light">
<tr>
<th>Type</th>
<th>Poids</th>
<th>Points</th>
<th>Date</th>
<th>Statut</th>
</tr>
</thead>
<tbody>
<% deposits.forEach(d => { %>
<tr>
<td>
<div class="d-flex align-items-center">
<div class="bg-<%= d.type === 'plastique' ? 'primary' : d.type === 'verre' ? 'success' : d.type === 'metal' ? 'warning' : 'secondary' %> rounded-circle d-flex align-items-center justify-content-center me-3"style="width: 40px; height: 40px;">
<i class="fas fa-<%= d.type === 'plastique' ? 'wine-bottle' : d.type === 'verre' ? 'glass-martini-alt' : d.type === 'metal' ? 'cube' : 'laptop' %> text-white"></i>
</div>
<span class="text-capitalize"><%= d.type %></span>
</div>
</td>
<td>
<strong><%= d.weight %> kg</strong>
</td>
<td>
<span class="badge bg-success rounded-pill">+<%= d.points %> pts
</span>
</td>
<td>
<small class="text-muted">
<%= new Date(d.created_at * 1000).toLocaleDateString() %>
</small>
</td>
<td>
<span class="badge bg-success">Validé</s
pan>
</td>
</tr>
<% }) %>
</tbody>
</table>
</div>
<!-- Résumé -->
<div class="row mt-4 pt-4 border-top"><div class="col-md-4 text-center">
<h4 class="fw-bold text-primary"><%= deposits.length %></h4>
<p class="text-muted mb-0">Dépôts total</p>
</div>
<div class="col-md-4 text-center">
<h4 class="fw-bold text-success">
<%= deposits.reduce((total, d) => total + d.points, 0) %>
</h4>
<p class="text-muted mb-0">Points gagnés</p>
</div>
<div class="col-md-4 text-center">
<h4 class="fw-bold text-warning">
<%= deposits.reduce((total, d) => total + parseFloat(d.weight), 0) %> kg
</h4>
<p class="text-muted mb-0">Déchets recyclés</p>
</div>
</div>
<% } %>
</div>
</div>
</div>
</div>
</main>
  <%- include('../partials/footer') %>

