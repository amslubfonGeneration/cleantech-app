<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CleanTech - Recyclage Intelligent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/m.css" />
</head>
<style>
:root {
--primary: #10b981;--primary-dark: #059669;
--primary-light: #d1fae5;
--secondary: #3b82f6;
--secondary-dark: #2563eb;
--accent: #f59e0b;
--dark: #1f2937;
--light: #f9fafb;
}
.dashboard-card {
background: white;
border-radius: 15px;
box-shadow: 0 4px 20px rgba(0,0,0,0.08);
transition: all 0.3s ease;
border: none;
overflow: hidden;
}
.dashboard-card:hover {
transform: translateY(-5px);
box-shadow: 0 8px 30px rgba(0,0,0,0.12);
}
.stat-card {
background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
color: white;
border-radius: 15px;
padding: 1.5rem;
}
.nav-pills-custom .nav-link {
border-radius: 10px;
margin: 0.2rem 0;
transition: all 0.3s ease;
}
.nav-pills-custom .nav-link.active {
background: var(--primary);
color: white;
}
</style>
</head>
<body>

<%- include('../../../partials/navbar', { user}) %>

  <br><br>
<main style="padding-top: 80px;">
<div class="container py-5">
<div class="row">
<div class="col-12">
<div class="d-flex justify-content-between align-items-center mb-5">
<div>
<h1 class="h2 fw-bold text-dark mb-2">Participants au défi</h1>
<p class="text-muted mb-0"><%= challenge.name %></p>
</div>
<a href="/challenges" class="btn btn-outline-secondary">
<i class="fas fa-arrow-left me-2"></i>Retour aux défis
</a>
</div>
</div>
</div>
<!-- Informations du défi -->
<div class="row mb-5">
<div class="col-12">
<div class="dashboard-card p-4">
<div class="row">
<div class="col-md-4">
<h6 class="fw-bold text-muted">Sponsor</h6>
<p class="h5"><%= challenge.sponsor || 0 %></p>
</div>
<div class="col-md-4"><h6 class="fw-bold text-muted">Bonus</h6>
<p class="h5 text-success"><%= challenge.bonus_points %> points</p>
</div>
<div class="col-md-4">
<h6 class="fw-bold text-muted">Période</h6>
<p class="mb-0">
Du <%= new Date(challenge.start_date * 1000).toLocaleDateString() %><br>
au <%= new Date(challenge.end_date * 1000).toLocaleDateString() %>
</p>
</div>
</div>
</div>
</div>
</div>
<!-- Liste des participants -->
<div class="row">
<div class="col-12">
<div class="dashboard-card p-4">
<div class="d-flex justify-content-between align-items-center mb-4">
<h4 class="fw-bold mb-0">👥 Participants (<%= participants.length %>)</h4>

<div class="text-muted">
<i class="fas fa-users me-1"></i>
<%= participants.length %> participant(s)
</div>
</div>
<% if (participants.length === 0) { %>
<div class="text-center py-5">
<i class="fas fa-users fa-4x text-muted mb-3"></i>
<h5 class="text-muted">Aucun participant</h5>
<p class="text-muted">Aucun utilisateur n'a encorerejoint ce défi.</p>
</div>
<% } else { %>
<div class="table-responsive">
<table class="table table-hover">
<thead class="table-light">
<tr>
<th>Nom</th>
<th>Email</th>
<th>Rôle</th>
<th>Date d'inscription</th>
</tr>
</thead>
<tbody>
<% participants.forEach(p => { %>
<tr>
<td>
<div class="d-flex align-items-center">
<div class="bg-primary rounded-circled-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px;">
<i class="fas fa-user text-white"></i>
</div>
<div>
<strong><%= p.name %></strong>
</div>
</div>
</td>
<td><%= p.email %></td>
<td>
<span class="badge bg-<%= p.role === 'admin' ? 'danger' : p.role === 'partner' ? 'warning' : p.role ==='investor' ? 'success' : 'primary' %>">
<%= p.role %>
</span></td>
<td>
<small class="text-muted">
<%= new Date(p.joined_at * 1000).toLocaleDateString() %>
</small>
</td>
</tr>
<% }) %>
</tbody>
</table>
</div>
<% } %>
</div>
</div>
</div>
</div>
</main>
  <%- include('../../../partials/footer') %>

