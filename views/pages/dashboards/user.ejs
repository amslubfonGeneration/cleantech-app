<!DOCTYPE html>
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CleanTech - Recyclage Intelligent</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/m.css" />
</head>
<style>
:root {
--primary: #10b981;--primary-dark: #059669;
--primary-light: #d1fae5;
--secondary: #3b82f6;
--secondary-dark: #2563eb;
--accent: #f59e0b;
--dark: #1f2937;
--light: #f9fafb;
}
.dashboard-card {
background: white;
border-radius: 15px;
box-shadow: 0 4px 20px rgba(0,0,0,0.08);
transition: all 0.3s ease;
border: none;
overflow: hidden;
}
.dashboard-card:hover {
transform: translateY(-5px);
box-shadow: 0 8px 30px rgba(0,0,0,0.12);
}
.stat-card {
background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
color: white;
border-radius: 15px;
padding: 1.5rem;
}
.nav-pills-custom .nav-link {
border-radius: 10px;
margin: 0.2rem 0;
transition: all 0.3s ease;
}
.nav-pills-custom .nav-link.active {
background: var(--primary);
color: white;
}
</style>
</head>
<body>
<%- include('../../partials/navbar', { user, active: 'dashboard'}) %>

<main style="padding-top: 80px;">
<div class="container py-4">
<!-- Header du dashboard -->
<div class="row mb-5">
<div class="col-12">
<div class="d-flex justify-content-between align-items-center">
<div>
<h1 class="h2 fw-bold text-dark mb-2">Tableau de bord</h1>
<p class="text-muted">Bonjour <strong><%= user.name%></strong>, suivez votre impact écologique</p>
</div>
<div class="bg-primary text-white rounded-pill px-3 py-1">
<small><i class="fas fa-user me-1"></i><%= user.role%></small>
</div>
</div>
</div>
</div>
<!-- Cartes de statistiques -->
<div class="row mb-4">
<div class="col-xl-3 col-md-6 mb-4">
<div class="stat-card text-white">
<div class="d-flex justify-content-between align-items-start">
<div>
<h3 class="h2 fw-bold mb-1"><%= points || 0 %></h3>
<p class="mb-0">Points cumulés</p>
</div>
<div class="bg-white bg-opacity-20 rounded-circle p-3">
<i class="fas fa-coins fa-lg"></i>
</div></div>
</div>
</div>
<div class="col-xl-3 col-md-6 mb-4">
<div class="stat-card text-white" style="background: linear-gradient(135deg, #f59e0b 0%, #f97316 100%);">
<div class="d-flex justify-content-between align-items-start">
<div>
<h3 class="h2 fw-bold mb-1"><%= referrals.length %></h3>
<p class="mb-0">Filleuls</p>
</div>
<div class="bg-white bg-opacity-20 rounded-circle p-3">
<i class="fas fa-users fa-lg"></i>
</div>
</div>
</div>
</div>
<div class="col-xl-3 col-md-6 mb-4">
<div class="stat-card text-white" style="background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);">
<div class="d-flex justify-content-between align-items-start">
<div>
<h3 class="h2 fw-bold mb-1"><%= userBadges.length %></h3>
<p class="mb-0">Badges</p>
</div>
<div class="bg-white bg-opacity-20 rounded-circle p-3">
<i class="fas fa-medal fa-lg"></i>
</div>
</div></div>
</div>
<div class="col-xl-3 col-md-6 mb-4">
<div class="stat-card text-white" style="background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);">
<div class="d-flex justify-content-between align-items-start">
<div>
<h3 class="h2 fw-bold mb-1"><%= deposits.length %>
</h3>
<p class="mb-0">Dépôts</p>
</div>
<div class="bg-white bg-opacity-20 rounded-circle p-3">
<i class="fas fa-recycle fa-lg"></i>
</div>
</div>
</div>
</div>
</div>
<!-- Actions rapides -->
<div class="row mb-5">
<div class="col-12">
<div class="dashboard-card p-4">
<h4 class="fw-bold mb-4">Actions rapides</h4>
<div class="row g-3">
<div class="col-md-3 col-6">
<a href="/rewards" class="btn btn-outline-primary w-100 d-flex flex-column align-items-center py-3">
<i class="fas fa-gift fa-2x mb-2"></i>
<span>Récompenses</span>
</a>
</div>
<div class="col-md-3 col-6">
<a href="/badges" class="btn btn-outline-secondaryw-100 d-flex flex-column align-items-center py-3">
<i class="fas fa-medal fa-2x mb-2"></i>
<span>Mes badges</span>
</a>
</div>
<div class="col-md-3 col-6">
<a href="/leaderboard/parrainage" class="btn btn-outline-info w-100 d-flex flex-column align-items-center py-3">
<i class="fas fa-trophy fa-2x mb-2"></i>
<span>Classement</span>
</a>
</div>
<div class="col-md-3 col-6">
<a href="/deposits" class="btn btn-outline-success w-100 d-flex flex-column align-items-center py-3">
<i class="fas fa-recycle fa-2x mb-2"></i>
<span>Mes dépôts</span>
</a>
</div>
</div>
</div>
</div>
</div>
<!-- Badges et Prochain badge -->
<div class="row">
<div class="col-lg-6 mb-4">
<div class="dashboard-card p-4 h-100">
<h5 class="fw-bold mb-3">🎖️ Vos Badges</h5>
<% if (userBadges.length === 0) { %>
<div class="text-center py-4">
<i class="fas fa-medal fa-3x text-muted mb-3"></i>
<p class="text-muted">Aucun badge pour le moment.
</p>
<p class="text-muted small">Participez à des défiset parrainez des amis pour gagner des badges !</p>
</div><% } else { %>
<div class="row g-3">
<% userBadges.forEach(b => { %>
<div class="col-12">
<div class="border rounded p-3">
<div class="d-flex justify-content-between align-items-start">
<div>
<h6 class="fw-bold mb-1"><%= b.name %></h6>
<p class="text-muted small mb-0"><%= b.description %></p>
</div>
<span class="badge bg-primary">Niveau <%= b.tier %></span>
</div>
</div>
</div>
<% }) %>
</div>
<% } %>
</div>
</div>
<div class="col-lg-6 mb-4">
<div class="dashboard-card p-4 h-100">
<h5 class="fw-bold mb-3">🎯 Prochain objectif</h5>
<% if (nextBadge && !alreadyEarned) { %>
<div class="text-center py-3">
<i class="fas fa-bullseye fa-3x text-primary mb-3"></i>
<h6 class="fw-bold"><%= nextBadge.name %></h6>
<p class="text-muted mb-3"><%= nextBadge.description %></p>
<div class="progress mb-3" style="height: 10px;">
<div class="progress-bar bg-primary" style= "width: <%= (parrainCount / nextBadge.requirement_points) * 100 %>"></div>
</div>
<p class="mb-0">
<strong><%= nextBadge.requirement_points - parrainCount %></strong> filleul(s) restant(s)</p>
</div>
<% } else { %>
<div class="text-center py-4">
<i class="fas fa-star fa-3x text-muted mb-3"></i>
<p class="text-muted">Aucun objectif en cours.</p>
<p class="text-muted small">Continuez à recycler et parrainez des amis pour débloquer de nouveaux défis !</p>
</div>
<% } %>
</div>
</div>
</div>
<!-- Parrainage -->
<div class="row mb-4">
<div class="col-12">
<div class="dashboard-card p-4">
<h5 class="fw-bold mb-3">🎉 Programme de parrainage</h5>
<p>Partagez ce lien pour inviter vos amis et gagner des points :</p>
<div class="input-group mb-3">
<input type="text" class="form-control" readonly value="<%= baseUrl %>:3000/?ref=<%= referral_code %>">
<button class="btn btn-outline-secondary" onclick="navigator.clipboard.writeText(this.previousElementSibling.value)">
<i class="fas fa-copy"></i>
</button>
</div><h6 class="fw-bold mt-4">Vos filleuls (<%= referrals.length %>)</h6>
<% if (referrals.length === 0) { %>
<p class="text-muted">Aucun filleul pour le moment.
</p>
<% } else { %>
<div class="table-responsive">
<table class="table table-hover">
<thead>
<tr>
<th>Nom</th>
<th>Email</th>
<th>Date</th>
</tr>
</thead>
<tbody>
<% referrals.forEach(r => { %>
<tr>
<td><%= r.name %></td>
<td><%= r.email %></td>
<td><small class="text-muted"><%= new Date(r.created_at * 1000).toLocaleDateString() %></small></td>
</tr>
<% }) %>
</tbody>
</table>
</div>
<% } %>
</div>
</div>
</div>
<!-- Derniers dépôts -->
<div class="row">
<div class="col-12">
<div class="dashboard-card p-4">
<h5 class="fw-bold mb-3">📦 Derniers dépôts</h5><% if (deposits.length === 0) { %>
<p class="text-muted">Aucun dépôt pour le moment.</p>
<a href="/deposit-points" class="btn btn-primary">
<i class="fas fa-map-marker-alt me-2"></i>Trouverun point de collecte
</a>
<% } else { %>
<div class="table-responsive">
<table class="table table-hover">
<thead>
<tr>
<th>Type</th>
<th>Poids</th>
<th>Points</th>
<th>Date</th>
</tr>
</thead>
<tbody>
<% deposits.slice(0, 5).forEach(d => { %>
<tr>
<td>
<span class="badge bg-<%= d.type === 'plastique' ? 'primary' : d.type === 'verre' ? 'success' : d.type === 'metal' ? 'warning' : 'secondary' %>">
<%= d.type %>
</span>
</td>
<td><%= d.weight %> kg</td>
<td><span class="fw-bold text-success"><%=d.points %> pts</span></td>
<td><small class="text-muted"><%= new Date(d.created_at * 1000).toLocaleDateString() %></small></td>
</tr>
<% }) %>
</tbody>
</table></div>
<% } %>
</div>
</div>
</div>
</div>
</main>





<%- include('../../partials/footer') %>